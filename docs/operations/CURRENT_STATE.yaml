# CURRENT_STATE — EXECUTAR Mobile validation
#
# Machine-updatable checkpoint for MOBILE-GATE-001..005. A new session must
# be able to continue by reading only this file + GATE_LOG.md +
# HANDOFF_CURRENT.md (if present) — never by re-deriving state from scratch.
# Overwrite this file in place after every gate-relevant action; do not
# append history here (that belongs in GATE_LOG.md / WORKFLOW_01_01_EXECUTION_LOG.md).

current_objective: >
  MOBILE-GATE-001 — produce an installable Android EAS build of apps/mobile,
  install it on a real device, and verify login (Clerk) -> Home ("Agora")
  loads real workspace data. Scanner is the priority after this gate closes
  (MOBILE-GATE-003/004). A parallel, lower-effort Web track re-confirms
  apps/web + apps/app production health.

head_sha: f8db4738a7348e961dcd7ed662f79f20afd49390
working_branch: claude/peaceful-fermi-b6hv5x
current_gate: MOBILE-GATE-001

completed:
  - "Bootstrap: read MASTER_WORKBOOK.md, AGENTS.md, DECISION_LOG.md, GATE_LOG.md,
     QUALITY_GATES.md, LAUNCH_RUNBOOK.md, PRODUCT_AUDIT.md, HANDOFF.md,
     WORKFLOW_01_01_EXECUTION_LOG.md, git log (2026-09-20)."
  - "Evidence-based audit of apps/mobile build config, Scanner pipeline, and
     all non-Scanner screens (Login/Home/Projects/Copilot/Mapa-OS/Reports/
     Settings) via 3 Explore agents — see summaries in this session's
     transcript and in GATE_LOG.md's next entry."
  - "Confirmed production backend is live: https://executar-nf-api.vercel.app/health
     -> 200; https://executar-nf-web.vercel.app/ -> 200;
     https://executar-nf-app.vercel.app/sign-in -> 200."
  - "CORRECTION (superseded, see blocked/user_action_required below): the
     Clerk publishable key found in production HTML
     (pk_live_LmNsZXJrLmFjY291bnRzLmRldiQ) is CORRUPTED, not safe to reuse.
     Base64-decoding its payload gives '.clerk.accounts.dev$' -- the
     instance subdomain segment is missing. Confirmed live impact:
     https://executar-nf-app.vercel.app/ returns HTTP 307 to
     'https://.accounts.dev/sign-in?...', a malformed host. Same secret-
     corruption pattern already seen with VERCEL_TOKEN/DATABASE_URL/
     RESEND_TOKEN/CLERK_WEBHOOK_SECRET (WORKFLOW_01_01_EXECUTION_LOG.md,
     2026-09-19). This key is shared by executar-nf-app/web/api (a prior
     session propagated app's value to web/api on 2026-09-19), so all
     three currently carry the corrupted value."
  - "Corrected doc drift: LAUNCH_RUNBOOK.md and INFRASTRUCTURE.md both
     claimed deploy-mobile.yml is gated on vars.EAS_PROJECT_CONFIGURED; that
     variable does not exist in the workflow (GATE_LOG.md FP-001 + direct
     read). Both docs now cite the real gate
     (scripts/load-deployment-config.sh mobile)."
  - "Confirmed Expo MCP connector exists and is authenticated at the org
     level (ListConnectors: installState=connected) but enabledInChat=false
     for this session — this is the single blocker for eas init/eas build,
     not a missing/invalid EXPO_TOKEN."
  - "BUILD-01 DONE: first real installable Android build of apps/mobile,
     build 627bfb17-6f10-4083-8fab-64b06d81489a, status FINISHED, APK at
     https://expo.dev/artifacts/eas/lUKYu12rWKO_-wKaI6FxUbGPVZVkgOZ4AQ3RUcbxf18.apk.
     Took 4 attempts; 3 real root causes found via actual build logs and
     fixed in order: missing expo-dev-client dependency, app.json slug
     mismatch vs. the real EAS project, and onnxruntime-react-native's
     Gradle script using a Gradle-internal API removed/unbound in Gradle
     9.3.1 (fixed via a committed bun patch, not a node_modules edit that
     would be wiped on reinstall)."

in_progress:
  - "CLERK_PLATFORM_API_KEY: user says they authorized/configured a Clerk
     API key for their organization (likely as an environment-level API
     credential, same mechanism used before for api.vercel.com/api.expo.dev
     in this repo's history). Tested 3 independent ways in this session --
     `clerk whoami`/`clerk apps list --json` (still 'Not logged in'),
     `clerk --verbose whoami` (confirms it never even checks
     CLERK_PLATFORM_API_KEY -- goes straight to keyring/credentials-file,
     meaning the env var is not set in this process), and a raw
     `curl https://api.clerk.com/v1/users` (plain 401, no proxy injection
     header at all, unlike the Vercel/Expo cases which showed an
     X-Proxy-Error when a rule existed but the token was rejected -- here
     there's no evidence any rule exists for this host at all). Matches
     this repo's own documented pattern (WORKFLOW_01_01_EXECUTION_LOG.md,
     2026-09-13): environment credential changes do not retroact into an
     already-running session, only a new one. See user_action_required."
  - "Web track: found (via mcp__Vercel__list_deployments metadata) that a
     PARALLEL session had already diagnosed the identical corrupted-Clerk-
     key bug independently and opened PR #32
     (fix(auth): reject Clerk publishable keys with an unresolvable host --
     packages/auth/keys.ts now validates the decoded Clerk key host at
     config time instead of shipping a broken redirect). User confirmed
     merge; PR #32 merged as f8db4738, then merged into this session's
     working branch (no conflicts). This does NOT fix production by itself
     -- it makes the next executar-nf-app/api deploy hard-fail until the
     real key is supplied, which is the deliberate, agreed trade-off."
  - "User asked to recover Clerk via a NEW, separate Clerk application
     ('EXECUTAR Echo', not reusing the corrupted one) using the real
     `clerk` CLI (npm package `clerk`, official, confirmed real via `npm
     view`/`--help` -- not hallucinated). Attempted `clerk auth login` from
     apps/app: this is a browser-OAuth flow with a LOCAL loopback callback
     (http://127.0.0.1:<port>/callback) -- confirmed by direct execution
     that this cannot complete from this remote/cloud container, since the
     user's own browser can never reach this container's 127.0.0.1. This
     is a structural environment limitation, not a retryable error.
     Reverse-engineered the compiled CLI binary (strings analysis) and
     found a real, documented-in-code non-interactive bypass: the
     `CLERK_PLATFORM_API_KEY` env var (prefix `ak_`) fully replaces
     `clerk auth login` for every subsequent command (apps list/create,
     link, env pull, doctor) -- confirmed by reading the actual auth-check
     logic in the binary, not guessed. See user_action_required below."

blocked:
  - "apps/app production: every signed-out visit to a protected route
     redirects to a malformed Clerk Account Portal URL
     (https://.accounts.dev/sign-in?...) -- reconfirmed deterministic (3/3
     curl attempts), not transient. PR #32 (merged) now makes future
     deploys fail loudly on this instead of shipping it silently, but
     production today still serves the last (broken-redirect) build until
     a corrected key triggers a new, passing deploy."
  - "apps/mobile Android build: Gradle-level native module incompatibility
     (onnxruntime-react-native vs. EAS's Gradle 9.3.1) -- see in_progress
     above. Not a credential/config issue this time; needs a real
     dependency-version or build-config fix."

user_action_required:
  - action: "The Clerk Platform API Key the user says they authorized is
      not reaching this session (tested 3 ways, see in_progress above).
      Two real options, pick one: (A) paste the `ak_...` value directly in
      chat -- session-scoped only, goes into a scratchpad temp file for
      the CLI to read via --input-json/env, never into a command string,
      never committed/logged, works immediately in THIS session; (B) if
      it was set as an environment-level credential, it needs a genuinely
      NEW Claude Code session to take effect (this exact limitation is
      already documented in WORKFLOW_01_01_EXECUTION_LOG.md from
      2026-09-13 for the same class of credential) -- continuation state
      for a new session is already kept current in this file + GATE_LOG.md
      for exactly this handoff."
    where: "Clerk Dashboard. The CLI's own fallback link is
      https://dashboard.clerk.com/last-active?path=api-keys -- if that
      page only shows pk_/sk_ keys for one application, look for an
      account-level 'Platform API Keys' or 'CLI' section instead (this is
      an account-wide credential, not scoped to one Clerk application --
      broader blast radius than a normal secret key, worth knowing before
      sharing it)."
    why: "`clerk auth login`'s browser OAuth needs a loopback callback on
      this container's own 127.0.0.1, which the user's browser can never
      reach from a separate machine -- confirmed by direct execution, not
      assumed. CLERK_PLATFORM_API_KEY is the CLI's own documented bypass
      for exactly this (CI/automation) case, confirmed by reading the
      compiled CLI binary's own auth-check logic."
    after: "Run the user's ECHO-CLERK-RECOVERY-001 sequence end-to-end:
      clerk apps list --json (evidence) -> clerk apps create \"EXECUTAR
      Echo\" -> clerk link --app <new_app_id> -> clerk env pull (dev only)
      -> clerk doctor --verbose -> decode & validate the new
      NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY -> report only the sanitized
      summary (app name, app_id, environment, decoded FAPI host,
      doctor result, PASS/FAIL) -- never the secret key. Then: write the
      corrected key into apps/mobile's eas.json env + a NEW EAS build
      (env is build-time, can't hot-swap the existing APK), plus into the
      3 Vercel projects + redeploy for the web track."

test_results:
  typecheck: "not re-run this session (last known green per PRODUCT_AUDIT.md
    / CI on main)"
  unit_tests: "not re-run this session"
  mobile_e2e: "none exist (confirmed: zero test files under apps/mobile)"

mobile_build:
  eas_project_id: "0479ea20-47e9-4e42-8b8a-c648e6f60b83" # real, pre-existing project confirmed via build_list
  eas_profile_planned: development
  platform_priority: "android first, then ios (needs Apple Developer + UDID)"
  status: FINISHED
  build_id: "627bfb17-6f10-4083-8fab-64b06d81489a"
  apk_url: "https://expo.dev/artifacts/eas/lUKYu12rWKO_-wKaI6FxUbGPVZVkgOZ4AQ3RUcbxf18.apk"
  blocker: "none for BUILD-01 itself; AUTH-01 still blocked on the corrupted Clerk key"

scanner_status:
  pipeline: "real, on-device (DINOv2 ONNX via onnxruntime-react-native),
    EXISTS-AND-WIRED end to end for dispatch/undo"
  model_artifact: "MISSING — no .onnx file anywhere, DINOV2_MODEL_URL/SHA256
    unset, no GitHub Release exists (DEC-004 OCR-vs-DINOv2 still A DEFINIR
    in DECISION_LOG.md)"
  symbol_enrollment: "MISSING — no API route or mobile screen calls
    registerSymbol() for a real workspace; only test fixtures do"
  known_bug: "packages/scanner/src/dispatch.ts:20-25 selects latest open
    task from {DOING,VERIFY}, but TASK_STATE_TRANSITIONS only allows
    VERIFY->DONE, not DOING->DONE (packages/schemas/src/task-state.ts:34-39)
    — dispatch errors instead of completing if the latest task is in DOING.
    Not fixed yet; scheduled for MOBILE-GATE-004 (SCN-09)."
  gate: "out of scope for GATE-001; targeted at GATE-003/004"

api_status:
  executar-nf-api: "production alias healthy (200 on /health); latest
    deployment attempt shows readyState=ERROR (likely the 2026-09-19 Vercel
    daily-deployment-quota exhaustion documented in
    WORKFLOW_01_01_EXECUTION_LOG.md — production alias still serves the
    last good build, unaffected)"
  executar-nf-web: "production alias healthy (200 on /)"
  executar-nf-app: "production alias healthy (200 on /sign-in; root / had a
    transient proxy tunnel error in this session's curl, not confirmed as
    an app-side failure — re-test before relying on it)"

auth_status:
  clerk_web: "REGRESSED since the 2026-09-19 log entry that called it
    healthy: that check only confirmed CLERK_SECRET_KEY/webhook-signature
    server-side, never a live browser sign-in. NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY
    is now confirmed corrupted (see blocked[] above) -- apps/app's
    sign-out redirect target is malformed in production right now."
  clerk_mobile: "code is real and wired (apps/mobile/app/_layout.tsx,
    (auth)/sign-in.tsx, (auth)/sign-up.tsx) but never run against a real
    device — blocked on the same corrupted publishable key; do not embed
    the current value in a build."

next_action: >
  Two independent user_action_required items block this gate: (1) enable
  the Expo connector for this chat, (2) get the correct, complete Clerk
  publishable key from the dashboard. Once (1) lands: eas init in
  apps/mobile -> populate app.json (projectId/updates.url/runtimeVersion).
  Once (2) lands: fix NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY in
  executar-nf-app/web/api via Vercel + redeploy, then set
  EXPO_PUBLIC_API_URL and the corrected EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY
  for the mobile build. Then: eas build --profile development --platform
  android -> hand the install link to the user -> verify login -> Home on
  the real device.

last_verified_at: "2026-09-20T08:52:00Z"
